# Express starter with ES6 syntax support

#### Tasks:

- Start: Start a live reload server (Ability to debug in VSCode)
- Build: Build the server in `dist` directroy.
- Clean: Remove `dist` directory